const cardStyles = { position: 'relative' };
const closeStyles = { position: 'absolute', right: 0, cursor: 'pointer' };
const positiveFeedbackStyles = { letterSpacing: '2px', marginTop: '10px', textAlign: 'center' };
const negativeFeedbackStyles = { letterSpacing: '2px', marginTop: '10px', textAlign: 'center' };
const responseStyles = { fontSize: '1.5em' };

export default {
  cardStyles,
  closeStyles,
  positiveFeedbackStyles,
  negativeFeedbackStyles,
  responseStyles
};
